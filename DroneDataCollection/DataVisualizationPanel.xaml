<UserControl x:Class="DroneDataCollection.DataVisualizationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DroneDataCollection"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>

    </UserControl.Resources>

    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    Margin="6,6,6,6">
            <Button Margin="3,3,3,3"
                    Content="渲染"
                    Click="onClickRender"/>
            <Button Margin="3,3,3,3"
                    Content="导出Csv"
                    Click="onClickExportCSV"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <local:DepthPropertyGrid SelectedObject="{Binding dataVisualization}">
            </local:DepthPropertyGrid>
        </ScrollViewer>
    </Grid>-->
    
    <ScrollViewer Grid.Row="1">
        <local:DepthPropertyGrid SelectedObject="{Binding dataVisualization}">
        </local:DepthPropertyGrid>
    </ScrollViewer>

</UserControl>