<local:DepthPropertyItem x:Class="DroneDataCollection.DepthPropertyGroupItem"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:local="clr-namespace:DroneDataCollection"
                         mc:Ignorable="d"
                         d:DesignHeight="450"
                         d:DesignWidth="800"
                         Margin="0,6,0,6"
                         Padding="6,6,6,0">

    <Expander Header="{Binding DisplayName}"
              IsExpanded="True">
        <Border BorderThickness="1,0,1,1"
                BorderBrush="{DynamicResource BorderBrush}"
                Background="{DynamicResource RegionBrush}"
                CornerRadius="0,0,4,4">
            <ContentPresenter Content="{Binding EditorElement}"/>
        </Border>
    </Expander>

</local:DepthPropertyItem>